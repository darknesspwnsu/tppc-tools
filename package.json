{
  "private": true,
  "engines": {
    "node": "^22.22.0 || ^24.0.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "sync:gold-data": "node scripts/sync-gold-data.mjs",
    "sync:gold-rarity": "node scripts/generate-gold-rarity.mjs",
    "sync:swap-status-data": "node scripts/sync-swap-status-data.mjs",
    "sync:pokesprite-data": "node scripts/sync-pokesprite-v2-manifest.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run test:ci",
    "test:unit": "vitest run",
    "test:e2e": "npm run build && playwright test tests/e2e",
    "test:parity": "npm run build && playwright test tests/parity",
    "test:parity:native": "npm run build && playwright test tests/parity",
    "test:parity:basepath": "NEXT_PUBLIC_BASE_PATH=/tppc-tools npm run build && NEXT_PUBLIC_BASE_PATH=/tppc-tools PARITY_BASE_PATH=/tppc-tools playwright test tests/parity",
    "test:ci": "npm run typecheck && npm run test:unit && npm run test:e2e && npm run test:parity && NEXT_PUBLIC_BASE_PATH=/tppc-tools npm run build",
    "capture:parity-fixtures": "tsx scripts/capture-parity-fixtures.ts",
    "codex:quick": "npm run typecheck && npm run test:unit",
    "codex:verify": "npm run test:ci"
  },
  "dependencies": {
    "next": "^16.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^22.13.5",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitest/coverage-v8": "^3.2.4",
    "jasmine": "^4.6.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  }
}
